# First offline run — preserved console summary

Evidence type: historical console excerpt reconstructed from the controller's contemporaneous progress record, **not the original JSON report**. The initial report was overwritten by a later run.

The first 69-case run failed three cases: two budget-oracle failures and one synthetic retrieval ranking failure. The budget assertion incorrectly required every alternative to be within budget; the corrected contract checks the truth of each `withinBudget` value. RAG evaluation now executes production `createPolicySearchService` with a deterministic retrieval adapter. This remains offline fixture evidence, not live model accuracy. The separate rank-only benchmark is unchanged.

The original per-case JSON and exact console lines are unavailable; no reconstructed case IDs or timings are claimed here.
